import { ArrayGeneratorPipe } from './array-generator.pipe';

describe('ArrayGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});
